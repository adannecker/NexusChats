/**
 * Reserved feature boundary for future local pinning. The first release keeps
 * this separate from selection so each productivity feature can evolve safely.
 */
/**
 * Placeholder boundary for future pin-related behavior.
 */
export class PinsFeature {
  readonly featureName = "pins";
}
